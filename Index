```react
import React, { useState, useEffect, useCallback } from 'react';
import { Heart, Sparkles, RefreshCw, Image as ImageIcon } from 'lucide-react';

// --- AS SUAS FOTOS E FRASES ---
// Os ficheiros carregados estão ligados diretamente aqui
const MOMENTOS = [
  {
    id: 1,
    image: "6d5d22e1-7eae-446e-a629-eef1b1bb4821-1_all_11410.jpg",
    quote: "Os nossos lanches a qualquer hora são sempre os meus encontros favoritos."
  },
  {
    id: 2,
    image: "6d5d22e1-7eae-446e-a629-eef1b1bb4821-1_all_10655.jpg",
    quote: "O teu sorriso é o meu lugar de paz no mundo."
  },
  {
    id: 3,
    image: "6d5d22e1-7eae-446e-a629-eef1b1bb4821-1_all_10534.jpg",
    quote: "Cada beijo teu faz o tempo parar por um instante."
  },
  {
    id: 4,
    image: "6d5d22e1-7eae-446e-a629-eef1b1bb4821-1_all_10483.jpg",
    quote: "De todos os caminhos que já andei, o que me levou até ti foi o mais bonito."
  },
  {
    id: 5,
    image: "6d5d22e1-7eae-446e-a629-eef1b1bb4821-1_all_9989.jpg",
    quote: "Contigo, qualquer passeio pela cidade vira uma aventura inesquecível."
  },
  {
    id: 6,
    image: "6d5d22e1-7eae-446e-a629-eef1b1bb4821-1_all_672.jpg",
    quote: "Você é o meu sol nos dias nublados e a minha calmaria."
  },
  {
    id: 7,
    image: "6d5d22e1-7eae-446e-a629-eef1b1bb4821-1_all_612.jpg",
    quote: "Sorte a minha de ter o amor da minha vida como a minha melhor companhia."
  },
  {
    id: 8,
    image: "6d5d22e1-7eae-446e-a629-eef1b1bb4821-1_all_9743.jpg",
    quote: "Cada detalhe teu é um motivo a mais para me apaixonar todos os dias."
  },
  {
    id: 9,
    image: "6d5d22e1-7eae-446e-a629-eef1b1bb4821-1_all_9235.jpg",
    quote: "Você é a minha notificação favorita da vida."
  },
  {
    id: 10,
    image: "6d5d22e1-7eae-446e-a629-eef1b1bb4821-1_all_8591.jpg",
    quote: "O melhor lugar do mundo é e sempre será onde tu estás."
  }
];

const FloatingParticles = () => {
  const [particles, setParticles] = useState([]);

  useEffect(() => {
    const interval = setInterval(() => {
      setParticles(prev => {
        const isHeart = Math.random() > 0.3; 
        const newParticle = {
          id: Math.random(),
          left: `${Math.random() * 100}%`,
          size: Math.random() * 20 + 10,
          animationDuration: `${Math.random() * 4 + 4}s`,
          type: isHeart ? 'heart' : 'sparkle',
          rotation: `${Math.random() * 360}deg`
        };
        return [...prev, newParticle].slice(-25); 
      });
    }, 500);

    return () => clearInterval(interval);
  }, []);

  return (
    <div className="fixed inset-0 pointer-events-none overflow-hidden z-0">
      {particles.map(p => (
        <div
          key={p.id}
          className="absolute bottom-[-50px] opacity-60 animate-float drop-shadow-md"
          style={{
            left: p.left,
            fontSize: `${p.size}px`,
            animationDuration: p.animationDuration,
            transform: `rotate(${p.rotation})`
          }}
        >
          {p.type === 'heart' ? (
            <Heart fill="currentColor" className="text-rose-400/70" />
          ) : (
            <Sparkles className="text-amber-300/80" fill="currentColor" />
          )}
        </div>
      ))}
    </div>
  );
};

const AnimatedBackground = () => (
  <div className="fixed inset-0 overflow-hidden pointer-events-none z-0">
    <div className="absolute top-[-10%] left-[-10%] w-96 h-96 bg-rose-400 rounded-full mix-blend-multiply filter blur-[100px] opacity-40 animate-blob"></div>
    <div className="absolute top-[20%] right-[-10%] w-96 h-96 bg-pink-300 rounded-full mix-blend-multiply filter blur-[100px] opacity-40 animate-blob animation-delay-2000"></div>
    <div className="absolute bottom-[-20%] left-[20%] w-96 h-96 bg-fuchsia-300 rounded-full mix-blend-multiply filter blur-[100px] opacity-40 animate-blob animation-delay-4000"></div>
  </div>
);

export default function App() {
  const [isOpen, setIsOpen] = useState(false);
  const [currentMoment, setCurrentMoment] = useState(null);
  const [isAnimating, setIsAnimating] = useState(false);
  const [isFirstLoad, setIsFirstLoad] = useState(true);

  const pickRandomMoment = useCallback(() => {
    setIsAnimating(true);
    
    setTimeout(() => {
      let nextMoment;
      do {
        const randomIndex = Math.floor(Math.random() * MOMENTOS.length);
        nextMoment = MOMENTOS[randomIndex];
      } while (MOMENTOS.length > 1 && currentMoment && nextMoment.id === currentMoment.id);
      
      setCurrentMoment(nextMoment);
      
      setTimeout(() => {
        setIsAnimating(false);
      }, 50);
      
    }, 600);
  }, [currentMoment]);

  const handleOpen = () => {
    setIsFirstLoad(false);
    setIsOpen(true);
    pickRandomMoment();
  };

  return (
    <div className="min-h-screen bg-gradient-to-br from-rose-50 via-pink-50 to-rose-100 flex flex-col items-center justify-center p-4 relative font-sans text-rose-900 overflow-hidden">
      
      {/* Estilos e animações globais */}
      <style dangerouslySetInnerHTML={{__html: `
        @import url('https://fonts.googleapis.com/css2?family=Dancing+Script:wght@600;700&family=Playfair+Display:ital,wght@0,400;0,600;1,400&display=swap');

        .font-cursive { font-family: 'Dancing Script', cursive; }
        .font-elegant { font-family: 'Playfair Display', serif; }

        @keyframes float {
          0% { transform: translateY(0) scale(0.8) rotate(0deg); opacity: 0; }
          10% { opacity: 0.8; }
          100% { transform: translateY(-120vh) scale(1.5) rotate(45deg); opacity: 0; }
        }
        
        @keyframes blob {
          0% { transform: translate(0px, 0px) scale(1); }
          33% { transform: translate(30px, -50px) scale(1.1); }
          66% { transform: translate(-20px, 20px) scale(0.9); }
          100% { transform: translate(0px, 0px) scale(1); }
        }

        .animate-float {
          animation-name: float;
          animation-timing-function: cubic-bezier(0.25, 0.46, 0.45, 0.94);
          animation-fill-mode: forwards;
        }

        .animate-blob {
          animation: blob 8s infinite;
        }
        .animation-delay-2000 { animation-delay: 2s; }
        .animation-delay-4000 { animation-delay: 4s; }

        .polaroid-shadow {
          box-shadow: 0 20px 40px -5px rgba(225, 29, 72, 0.15), 0 15px 20px -10px rgba(225, 29, 72, 0.1);
        }

        .card-transition {
          transition-property: all;
          transition-timing-function: cubic-bezier(0.34, 1.56, 0.64, 1);
          transition-duration: 800ms;
        }
        
        .card-exit {
          transition-property: all;
          transition-timing-function: cubic-bezier(0.4, 0, 1, 1);
          transition-duration: 500ms;
        }
      `}} />

      <AnimatedBackground />
      {(isOpen || !isFirstLoad) && <FloatingParticles />}

      <div className="z-10 w-full max-w-md mx-auto flex flex-col items-center justify-center min-h-[650px]">
        
        {!isOpen ? (
          <div className="flex flex-col items-center transform transition-all duration-1000 hover:scale-105">
            <div className="relative">
              <div className="absolute inset-0 bg-rose-400 rounded-full blur-xl opacity-40 animate-pulse"></div>
              
              <button 
                onClick={handleOpen}
                className="group relative flex flex-col items-center justify-center w-48 h-48 bg-white/90 backdrop-blur-sm rounded-full shadow-2xl border border-white/50 hover:shadow-rose-400/40 transition-all duration-500 overflow-hidden z-10"
              >
                <div className="absolute inset-0 bg-gradient-to-tr from-rose-100 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
                <Heart className="w-16 h-16 text-rose-500 mb-3 group-hover:scale-110 group-hover:fill-rose-500 transition-all duration-500 z-10 drop-shadow-md" />
                <span className="text-rose-800 font-elegant font-semibold tracking-wide z-10 text-lg text-center leading-tight">Abrir<br/>Cápsula</span>
              </button>
            </div>
            <p className="mt-10 text-rose-700/80 font-cursive text-3xl drop-shadow-sm">
              Para o meu amor...
            </p>
          </div>
        ) : (
          <div className="flex flex-col items-center w-full">
            
            <div 
              className={`bg-[#faf9f6] p-4 pb-10 rounded-sm polaroid-shadow border border-gray-100 transform origin-bottom 
                ${isAnimating 
                  ? 'card-exit opacity-0 translate-y-20 -rotate-12 scale-90' 
                  : 'card-transition opacity-100 translate-y-0 rotate-[-2deg] scale-100'
                } w-full max-w-sm`}
            >
              {/* Fita adesiva no topo */}
              <div className="absolute -top-3 left-1/2 transform -translate-x-1/2 w-24 h-6 bg-white/40 backdrop-blur-sm border border-white/20 rotate-[-2deg] shadow-sm z-20" style={{ backgroundImage: 'linear-gradient(to right, rgba(255,255,255,0.1), rgba(255,255,255,0.5), rgba(255,255,255,0.1))' }}></div>

              <div className="aspect-square w-full bg-rose-50 overflow-hidden relative mb-6 shadow-[inset_0_2px_4px_rgba(0,0,0,0.06)] flex items-center justify-center">
                {currentMoment?.image ? (
                  <img 
                    src={currentMoment.image} 
                    alt="O nosso momento" 
                    className="w-full h-full object-cover filter contrast-[1.05] brightness-[1.02]"
                    onError={(e) => {
                        e.target.style.display = 'none';
                        e.target.nextElementSibling.style.display = 'flex';
                    }}
                  />
                ) : null}
                
                <div className="hidden absolute inset-0 w-full h-full flex-col items-center justify-center text-rose-300 bg-rose-50 p-4 text-center">
                  <ImageIcon className="w-12 h-12 mb-2" />
                  <span className="text-sm font-sans">Aguardando a fotografia...</span>
                </div>
              </div>
              
              <div className="px-4 text-center mt-4">
                <p className="text-2xl font-cursive text-slate-800 leading-snug">
                  {currentMoment?.quote}
                </p>
              </div>
            </div>

            <div className={`mt-12 flex flex-col items-center gap-4 transition-all duration-700 delay-300 ${isAnimating ? 'opacity-0 translate-y-4' : 'opacity-100 translate-y-0'}`}>
              <button 
                onClick={pickRandomMoment}
                disabled={isAnimating}
                className="flex items-center gap-3 px-8 py-4 bg-white/80 backdrop-blur-md border border-rose-100 hover:bg-rose-50 active:bg-rose-100 text-rose-600 rounded-full shadow-lg hover:shadow-rose-200/50 transition-all font-elegant font-semibold text-lg disabled:opacity-50 disabled:cursor-not-allowed group"
              >
                <RefreshCw className={`w-5 h-5 text-rose-500 group-hover:rotate-180 transition-transform duration-500 ${isAnimating ? 'animate-spin' : ''}`} />
                <span>Sortear outra memória</span>
              </button>
            </div>

          </div>
        )}
      </div>
    </div>
  );
}


```
